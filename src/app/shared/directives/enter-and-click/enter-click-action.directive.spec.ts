import { EnterClickActionDirective } from './enter-click-action.directive';

describe('EnterAndClickDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterClickActionDirective();
    expect(directive).toBeTruthy();
  });
});
